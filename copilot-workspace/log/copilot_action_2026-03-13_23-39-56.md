# Copilot Action Log — 2026-03-13_23-39-56

## User Request
Fix 7 critical Terraform issues in `/infrastructure/`:

1. Replace hardcoded subscription ID in `versions.tf` with variable
2. Replace hardcoded personal IP in `modules/key_vault/main.tf` with variable
3. Remove hardcoded personal info from APIM module defaults and tags
4. Remove unused `azuread_client_config` data source from `main.tf`
5. Change ACR admin default from `true` to `false`
6. Enable Key Vault purge protection
7. Add TODO comments for Cosmos DB key-based auth migration

## Changes Made

| # | File | Change |
|---|------|--------|
| 1 | `versions.tf` | Replaced hardcoded subscription ID with `var.subscription_id` |
| 1 | `variables.tf` | Added `subscription_id` variable (no default, required) |
| 2 | `modules/key_vault/main.tf` | Replaced hardcoded IP with `var.network_acl_ip_rules`; added comment about `default_action = "Allow"` making ip_rules redundant |
| 2 | `modules/key_vault/variables.tf` | Added `network_acl_ip_rules` variable (`list(string)`, default `[]`) |
| 3 | `modules/apim/main.tf` | Replaced hardcoded tags block with `tags = var.tags` |
| 3 | `modules/apim/variables.tf` | Removed defaults from `apim_name`, `admin_email`, `organization_name`; added `tags` variable |
| 4 | `main.tf` | Removed unused `data "azuread_client_config" "current" {}` |
| 5 | `variables.tf` | Changed `container_registry_admin_enabled` default to `false` |
| 6 | `main.tf` | Changed `purge_protection_enabled` to `true`, removed "enable in production" comment |
| 7 | `main.tf` | Added TODO comments at Cosmos DB key vault secret and COSMOS_KEY env block |
